# Checkpoint — 2026-09-03 08:04 [session]

## Session summary
Continuation of the long win-nice (Windows `nice`/`renice`/`cpulimit`-style npm package) session. This chunk covered two full review-fix cycles plus a breaking rename, all committed locally (nothing pushed).

**Round 10** (review of `e6a30dc`): implemented the previously-deferred `#81`/`#82` P3 items via a background `ox` (Opus) design agent followed by two sequential `sh` (Sonnet) implementation agents — replaced the per-branch `CloseHandle` repetition in all 11 embedded C# launchers with a single ownership `try/finally`, added a fault-injection test harness (AST-extracts each launcher's real C# out of `bin/*.ps1`, swaps `[DllImport]` declarations for instrumented stubs, zero production change) covering `WaitForSingleObject`/`ResumeThread`/`AssignProcessToJobObject`/`SetInformationJobObject`/`GetExitCodeProcess` failure branches, and added `test/run-elevated.ps1` (`npm run test:elevated`) — a single-UAC entry point that self-elevates once and runs the whole Pester suite inside that session, activating the 3 previously-`Skipped` `admin.ps1` already-elevated cases. A self-generated review of that commit (`e6a30dc`, round 10) found 2 P2s (an elevated-helper token-check bug letting `-SelfElevated` be trusted without real elevation, and `TerminateProcess`'s own result being silently discarded) and 2 P3s (fault probes only covering 2 of 11 files; README not marking `test/` as source-checkout-only) — all four fixed directly (not via sub-agents) and verified (161 passed/0 failed/3 skipped).

Mid-session the user asked to **rename `cap`→`capc` and `pint`→`capt`**, bump the version **0.1.1→0.2.0** (0.1.1 was never tagged/published — it and the round-10 work landed together under 0.2.0), and update the installer to clean up old-named files on upgrade. Investigation found `install.js`'s `cleanupStaleFiles()` already handled that case (just needed a regression test, since it had zero coverage). Given the `capm` name-collision risk (shares the `cap` prefix — a naive substring rename would corrupt it into `capcm`), the user explicitly authorized doing the rename via `sh` sub-agents: one did the mechanical rename (`git mv` + all code/test references, with an explicit "cap=tool-name vs cap=English-word" judgment rule in the prompt), a second rewrote README/CONTRIBUTING/SKILL.md/bug_report.md prose. Both verified independently afterward (161 passed/0 failed/3 skipped, 55 passed/0 failed Node suite, zero `CapLauncher`/`PintLauncher` hits, zero `capcm`/`captm` corruption). Everything committed as two commits (review doc + fix/rename combined, since the same files were touched by both rounds and couldn't be cleanly split after the fact — a git-add mistake mid-commit was caught and fixed via `git reset --soft` before anything was pushed).

**Round 11** (review of `9d624fa`, the rename+round-10-fix commit): found 3 P2s and 2 P3s, all fixed directly:
1. CHANGELOG referenced a `v0.1.1` that was never tagged/published — merged into `[0.2.0]`, compare link fixed to `v0.1.0...v0.2.0`.
2. A package upgrade didn't refresh a previously-`skill install`-ed copy, leaving it recommending deleted `cap`/`pint` names — added `updateInstalledSkill()` (refreshes only an *existing* marked copy, stays opt-in, never touches foreign files), wired into `install()`.
3. Upgrade without a readable manifest left all 6 old-named files behind — `cleanupStaleFiles()` now falls back to a marker-based `binDir` scan (mirroring `uninstall()`'s existing fallback) when the manifest is missing/corrupt.
4. (P3) Fault-injection failure branches were still hardcoded to only `idle`/`capc` — parametrized across all 11 launchers (Job-only fault flags across just the 3 Job-shape ones) via a new `Invoke-LauncherProbe` helper; two now-redundant single-launcher success-path tests were removed.
5. (P3) `publish.yml` never verified the actual packed artifact — added `scripts/release-check.js` (`npm run release-check`): packs the real tarball, installs into an isolated `WIN_NICE_HOME`, verifies manifest/launcher names, smoke-tests exit-code propagation, cross-checks CHANGELOG heading/compare-tag against real git tags. Ran it locally, all 5 checks passed. Wired into `publish.yml` before `npm publish`.

**A real bug was caught and fixed while testing item 2 above**: `test/install-uninstall.test.js`'s `withHome()` helper only isolated `WIN_NICE_HOME`, not the separate `WIN_NICE_SKILL_HOME` env var that `skill.js` reads — since `install()` now also touches skill files, every `install()` call in that test file was reading/writing the user's *real* `~/.claude/skills` and `~/.agents/skills`. Confirmed via mtime that it actually happened once (harmlessly — the content that landed was current/correct, just written earlier than intended by a real user action). Fixed `withHome()` to isolate both env vars; confirmed via mtime that a re-run no longer touches the real files.

Both review-fix rounds followed the established "review doc gets its own separate commit" pattern. Everything is committed locally (`fbc7f8c` is HEAD); nothing has been pushed to `origin/master`.

## Active goal
None — no `/goal` Stop hook in force this session.

## TaskList
### pending
- #70 Manual: run `npm run test:elevated` (one UAC prompt) before tagging v0.2.0 — user's own action, no UAC interaction available in this environment.
- #76 Final pre-tag verification + create annotated v0.2.0 tag (blockedBy: #70)

### recently completed
- #91-95: round-11 review fixes (CHANGELOG merge, skill-update-on-upgrade, manifest-fallback cleanup, fault-injection parametrization, release-check script)
- #84-90: round-10 review fixes + cap→capc/pint→capt rename + version 0.2.0 + docs rewrite
- #81-83: SafeHandle-refactor design + try/finally implementation + fault-injection harness + run-elevated.ps1
- #67-80: earlier review-fix rounds (capm huge-size parsing, nested-memory docs, publish.yml hardening, native Win32 return-value checks, pint affinity tests)

## Decisions
- Version is `0.2.0` (not `0.1.1`) — the rename is a breaking change, and `0.1.1` was never actually tagged/published, so its content was folded into `0.2.0` rather than kept as a separate historical CHANGELOG section.
- `cap`→`capc`, `pint`→`capt` rename executed via `sh` sub-agents (explicit user authorization) specifically because of the `capm`-collision corruption risk and the sheer file count (~15 files, ~2000 lines in `test/win-nice.Tests.ps1` alone) — both agents' work was independently re-verified afterward, not just trusted.
- `updateInstalledSkill()` only ever refreshes an *existing* marked copy — deliberately never auto-installs the skill for a user who hasn't opted in, matching the project's existing marker-based safety conventions.
- `scripts/release-check.js` uses `execFileSync('npm', ..., { shell: true })` (via an `npmExec` wrapper) despite the shell-escaping deprecation warning — verified that both alternatives (bare `npm` without shell, and explicit `npm.cmd` without shell) fail outright on this Windows/Node setup (ENOENT and EINVAL respectively); every argument passed through it is a fixed literal or a script-generated path, never external input, so the warning doesn't correspond to a real risk here.
- Standing rules unbroken throughout: no version bump beyond the explicit 0.2.0 request, no commit without explicit "коммит", no push, sub-agents only used because explicitly authorized for the rename specifically.

## Open questions
- None outstanding from the user. Waiting on: the user's own `npm run test:elevated` run (#70, needs a real UAC click) before `v0.2.0` can be tagged (#76).

## Repo state
```
(clean)
```
```
fbc7f8c Fix 9d624fa round-11 review findings (3 P2, 2 P3)
e80def1 Document 22:42 review of 9d624fa
9d624fa Fix e6a30dc round-10 review findings; rename cap->capc, pint->capt (0.2.0)
aa5a071 Document 20:34 review of e6a30dc
e6a30dc Implement #81/#82: single try/finally handle cleanup + fault-injection tests; add single-UAC elevated test entry point
```
